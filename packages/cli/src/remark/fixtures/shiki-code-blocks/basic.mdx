# Code Block Fixtures

This slide has a plain code block (no step-through).

```typescript
const x = 1
const y = 2
console.log(x + y)
```

And a code block with step-through:

```typescript {1|3}
const a = 'hello'
const b = 'world'
console.log(a, b)
```
