---
title: Mermaid demo
---

import './styles.css'
import { Mermaid } from './components/Mermaid'

# Mermaid in user space

Honeydeck renders this diagram through a deck-local React component.

<Mermaid>
flowchart LR
  A[Install mermaid] --> B[Create component]
  B --> C[Import in MDX]
  C --> D[Render diagram]
</Mermaid>

---

# Styling with tokens

The component and CSS live in this project, so you can decide how closely the diagram follows your theme.

<div className="grid gap-3">
<div className="grid grid-cols-4 gap-2 overflow-hidden rounded-honeydeck border border-border bg-surface p-2 max-[900px]:grid-cols-2" aria-label="Honeydeck theme colors used by the diagram">
  <div className="grid min-w-0 grid-cols-[2rem_auto_minmax(0,1fr)] items-center gap-2 text-[14px] leading-[1.2] text-foreground">
    <span className="h-8 w-8 rounded-honeydeck border border-border" style={{ background: 'var(--honeydeck-primary)' }} />
    <span>Primary</span>
    <code className="bg-transparent p-0 font-mono text-[12px] text-surface-foreground">--honeydeck-primary</code>
  </div>
  <div className="grid min-w-0 grid-cols-[2rem_auto_minmax(0,1fr)] items-center gap-2 text-[14px] leading-[1.2] text-foreground">
    <span className="h-8 w-8 rounded-honeydeck border border-border" style={{ background: 'var(--honeydeck-surface)' }} />
    <span>Surface</span>
    <code className="bg-transparent p-0 font-mono text-[12px] text-surface-foreground">--honeydeck-surface</code>
  </div>
  <div className="grid min-w-0 grid-cols-[2rem_auto_minmax(0,1fr)] items-center gap-2 text-[14px] leading-[1.2] text-foreground">
    <span className="h-8 w-8 rounded-honeydeck border border-border" style={{ background: 'var(--honeydeck-accent)' }} />
    <span>Accent</span>
    <code className="bg-transparent p-0 font-mono text-[12px] text-surface-foreground">--honeydeck-accent</code>
  </div>
  <div className="grid min-w-0 grid-cols-[2rem_auto_minmax(0,1fr)] items-center gap-2 text-[14px] leading-[1.2] text-foreground">
    <span className="h-8 w-8 rounded-honeydeck border border-border" style={{ background: 'var(--honeydeck-foreground)' }} />
    <span>Foreground</span>
    <code className="bg-transparent p-0 font-mono text-[12px] text-surface-foreground">--honeydeck-foreground</code>
  </div>
</div>

<Mermaid>
sequenceDiagram
  participant Deck
  participant Component
  participant Mermaid
  Deck->>Component: chart source
  Component->>Mermaid: render()
  Mermaid-->>Component: SVG
</Mermaid>
</div>
